<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Recipes_app.ViewModels"
             x:Class="Recipes_app.Views.RegisterPage"
             x:DataType="vm:RegisterViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{AppThemeBinding Light={StaticResource BackgroundLight}, Dark={StaticResource BackgroundDark}}">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="18" VerticalOptions="Center">

            <!-- Header -->
            <VerticalStackLayout Spacing="5" HorizontalOptions="Center" Margin="0,30,0,15">
                <Label Text="Create Account"
                      Style="{StaticResource HeroTitleLabel}"
                      FontSize="28"
                      TextColor="{StaticResource PrimaryDeep}"
                       HorizontalOptions="Center" />
                <Label Text="Join FlavorVault and start cooking!"
                      Style="{StaticResource MutedCaptionLabel}"
                      FontSize="14"
                       HorizontalOptions="Center" />
            </VerticalStackLayout>

            <!-- Error Message -->
            <Border IsVisible="{Binding HasError}"
                    BackgroundColor="#FFF3F0"
                    Stroke="{StaticResource Error}"
                    StrokeShape="RoundRectangle 8"
                    Padding="12">
                <Label Text="{Binding ErrorMessage}"
                       TextColor="{StaticResource Error}"
                       FontSize="13" />
            </Border>

            <!-- Display Name -->
            <VerticalStackLayout Spacing="6">
                <Label Text="Full Name" FontSize="14" FontAttributes="Bold"
                       TextColor="{AppThemeBinding Light={StaticResource OnBackgroundLight}, Dark={StaticResource OnBackgroundDark}}" />
                <Border Style="{StaticResource ElevatedCardBorder}" StrokeShape="RoundRectangle 10" Padding="4">
                    <Entry Text="{Binding DisplayName}" Placeholder="Enter your name" FontSize="16" />
                </Border>
            </VerticalStackLayout>

            <!-- Email -->
            <VerticalStackLayout Spacing="6">
                <Label Text="Email" FontSize="14" FontAttributes="Bold"
                       TextColor="{AppThemeBinding Light={StaticResource OnBackgroundLight}, Dark={StaticResource OnBackgroundDark}}" />
                <Border Style="{StaticResource ElevatedCardBorder}" StrokeShape="RoundRectangle 10" Padding="4">
                    <Entry Text="{Binding Email}" Placeholder="Enter your email" Keyboard="Email" FontSize="16" />
                </Border>
            </VerticalStackLayout>

            <!-- Password -->
            <VerticalStackLayout Spacing="6">
                <Label Text="Password" FontSize="14" FontAttributes="Bold"
                       TextColor="{AppThemeBinding Light={StaticResource OnBackgroundLight}, Dark={StaticResource OnBackgroundDark}}" />
                <Border Style="{StaticResource ElevatedCardBorder}" StrokeShape="RoundRectangle 10" Padding="4">
                    <Entry Text="{Binding Password}" Placeholder="Min 6 characters" IsPassword="True" FontSize="16" />
                </Border>
            </VerticalStackLayout>

            <!-- Confirm Password -->
            <VerticalStackLayout Spacing="6">
                <Label Text="Confirm Password" FontSize="14" FontAttributes="Bold"
                       TextColor="{AppThemeBinding Light={StaticResource OnBackgroundLight}, Dark={StaticResource OnBackgroundDark}}" />
                <Border Style="{StaticResource ElevatedCardBorder}" StrokeShape="RoundRectangle 10" Padding="4">
                    <Entry Text="{Binding ConfirmPassword}" Placeholder="Re-enter password" IsPassword="True" FontSize="16" />
                </Border>
            </VerticalStackLayout>

            <!-- Register Button -->
            <Button Text="Create Account"
                    Command="{Binding RegisterCommand}"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="White"
                    FontAttributes="Bold"
                    FontSize="16"
                    CornerRadius="12"
                    HeightRequest="50" />

            <ActivityIndicator IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}"
                               Color="{StaticResource Primary}"
                               HorizontalOptions="Center" />

            <!-- Login Link -->
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="4" Margin="0,10,0,0">
                <Label Text="Already have an account?"
                       FontSize="14"
                       TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray300}}" />
                <Label Text="Login"
                       FontSize="14"
                       FontAttributes="Bold"
                       TextColor="{StaticResource Primary}">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoToLoginCommand}" />
                    </Label.GestureRecognizers>
                </Label>
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
